kind = "web"
previewPath = "/"
title = "RedPath Sentinel"
version = "1.0.0"
id = "artifacts/redpath-sentinel"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18299

[services.development]
run = "pnpm --filter @workspace/redpath-sentinel run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/redpath-sentinel", "run", "build" ]
publicDir = "artifacts/redpath-sentinel/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18299"
BASE_PATH = "/"
